import { Devs } from "@utils/constants";
import definePlugin from "@utils/types";

export default definePlugin({
    name: "ReviewDB",
    description: "Review other users on Discord",
    authors: [Devs.mantikafasi],
});
